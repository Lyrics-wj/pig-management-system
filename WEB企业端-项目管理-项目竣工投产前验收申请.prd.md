# WEB企业端 - 项目管理 - 项目竣工投产前验收申请

> **文档版本**：v1.0  
> **创建时间**：2026-03-04  
> **文档状态**：待评审  
> **关联管理端PRD**：`doc_prd/WEB/管理端/WEB管理端-项目管理-项目竣工投产前验收申请.prd.md`  
> **前置依赖**：`doc_prd/WEB/企业端/WEB企业端-项目管理-畜牧项目建设申报.prd.md`（申报流程完结后方可发起验收申请）

---

> **⚠️ 多功能 PRD 说明**
>
> 本文档包含 **3 个独立功能**，按照 PRD 设计规范 v1.6「多功能 PRD 编写规范」完整编写 3.1～3.6 全部章节。
>
> | 功能编号 | 功能名称 |
> |---------|---------|
> | 功能一 | 验收申请入口与资格校验 |
> | 功能二 | 竣工投产前验收申请信息填报 |
> | 功能三 | 验收申请进度跟踪与结果查看 |

---

## 功能一：验收申请入口与资格校验

### 1.1 基本信息

- **功能名称**：验收申请入口与资格校验
- **关联现有模块/功能**：
  - 用户认证：复用现有 `/login`、`/getInfo`、`/getRouters` 接口与 JWT 机制
  - 畜牧项目建设申报：依赖 `project_application` 表，校验申报状态为 `COUNTY_APPROVED`
  - 参考文档：`doc_spec/现有业务说明.spec.md`
- **目标用户/场景**：

| 用户角色 | 使用场景 | 主要需求 |
|---------|---------|---------|
| 养殖场申报主体（企业端用户） | 完成项目建设后，发起竣工投产前验收申请 | 在已完结的申报项目列表中找到对应项目，点击"发起验收申请"进入填报流程 |

### 1.2 目标与范围

**目标**：
1. 在"我的申报"列表或申报详情页中，对状态为 `COUNTY_APPROVED`（县审核通过，申报完结）的申报提供"发起验收申请"入口。
2. 点击前校验该申报是否已存在未完结的验收申请记录（验收申请状态不为 `WITHDRAWN` 且不为 `COUNTY_APPROVED`），若存在则跳转至验收进度页（功能三），不允许重复发起。
3. 若已有验收申请且状态为 `COUNTY_APPROVED`（验收通过），则隐藏"发起验收申请"按钮，展示"验收已通过"标签。

**非目标（Non-Goals）**：
- ❌ 不提供独立的验收申请通知浏览功能（验收入口直接在申报详情页/申报列表页展示）。
- ❌ 不支持未经申报直接发起验收申请。

### 1.3 现状与复用

**现状简述**：
- 验收申请入口依附于已有的"我的申报"列表页（企业端PRD功能三），在申报详情页新增操作按钮，无需新增独立页面。
- 验收申请主记录表 `project_acceptance` 为新建表。

**复用检查结论**：

| 检查项 | 现有资源 | 复用结论 | 说明 |
|--------|---------|---------|------|
| 用户认证接口 | `/login`、`/getInfo`、`/getRouters` | ✅ 直接复用 | 见现有业务说明.spec.md 二、认证与通用 |
| 申报主记录表 | `project_application`（申报PRD新建） | ✅ 直接复用 | 查询 `COUNTY_APPROVED` 状态申报作为验收入口前提 |
| 验收申请查询接口 | 无 | 🆕 新增 `/project/acceptance/my/{applicationId}` | 查询指定申报的验收申请记录（用于入口资格校验） |
| 验收申请主记录表 | 无 | 🆕 新建表 `project_acceptance` | 存储验收申请主记录 |

### 1.4 功能需求

**功能列表**：

| 功能点 | 优先级 | 说明 |
|-------|-------|------|
| 申报详情页新增验收入口 | P0 | 申报状态为 `COUNTY_APPROVED` 时，详情页底部操作区展示"发起验收申请"按钮 |
| 验收申请资格校验 | P0 | 点击"发起验收申请"前查询是否已有验收申请记录，决定跳转目标 |
| 重复申请拦截 | P0 | 已有进行中验收申请时，跳转至验收进度页；验收已通过时展示"验收已通过"标签 |

**关键流程**：

```
企业端"我的申报"列表或申报详情页
        ↓
申报状态为 COUNTY_APPROVED
        ↓
展示"发起验收申请"按钮
        ↓
点击按钮 → 查询该申报的验收申请记录
        ↓
    ┌───────────────────────────────┐
    ↓           ↓                   ↓
无验收申请    已有进行中验收申请    验收申请状态为COUNTY_APPROVED
    ↓          （非WITHDRAWN）          ↓
跳转至验收      跳转至验收进度页     展示"验收已通过"标签
  填报页                              不展示按钮
```

#### 1.4.1 功能流程逻辑

**按钮展示规则**（申报详情页底部操作区）：
- 申报状态为 `COUNTY_APPROVED` + 无验收申请记录 → 展示"发起验收申请"按钮（主色按钮）
- 申报状态为 `COUNTY_APPROVED` + 有验收申请且状态不为 `COUNTY_APPROVED` 且不为 `WITHDRAWN` → 展示"查看验收进度"按钮（次要按钮）
- 申报状态为 `COUNTY_APPROVED` + 有验收申请且状态为 `COUNTY_APPROVED` → 展示"验收已通过"绿色标签（非按钮），不可再次发起
- 申报状态不为 `COUNTY_APPROVED` → 不展示任何验收相关按钮

#### 1.4.2 数据模型与字段定义

**核心实体：验收申请主记录表（`project_acceptance`）**

| 字段名（中文） | 字段名（英文） | 类型 | 长度 | 必填 | 默认值 | 取值范围/枚举值 | 说明 |
|-------------|-------------|------|------|------|-------|--------------|------|
| 验收申请ID | id | bigint | — | 是 | 自增 | — | 主键 |
| 关联申报ID | application_id | bigint | — | 是 | — | — | 外键，关联 `project_application.id`，唯一约束（同一申报只能有一条非WITHDRAWN验收记录） |
| 申请单位名称 | company_name | varchar | 200 | 是 | — | 最大长度200字符 | 从申报记录自动回显，只读 |
| 申请时间 | apply_date | date | — | 是 | 提交时当前日期 | — | 系统自动生成，只读 |
| 验收申请状态 | acceptance_status | varchar | 30 | 是 | `DRAFT` | 见1.4.1及功能二2.4.1状态定义（共13种取值） | — |
| 申报用户ID | applicant_user_id | bigint | — | 是 | — | — | 登录用户ID，系统自动带入，只读 |
| 创建时间 | create_time | datetime | — | 是 | 当前时间 | — | 自动生成，只读 |
| 更新时间 | update_time | datetime | — | 是 | 当前时间 | — | 自动更新，只读 |
| 删除标志 | del_flag | char | 1 | 是 | `0` | `0` 未删除、`2` 已删除（仅此2种取值） | 逻辑删除 |

#### 1.4.3 界面交互逻辑

- 验收入口按钮紧贴申报详情页底部操作区，位于原有按钮右侧（若无其他按钮，独占操作区）。
- 查询验收申请记录时展示短暂加载状态（500ms内完成），防止重复点击。
- 跳转至验收填报页时，将申报ID（`application_id`）作为路由参数传入。

#### 1.4.4 接口路径定义

| 接口名称 | HTTP 方法 | 路径 | 请求参数摘要 | 响应数据摘要 | 备注 |
|----------|-----------|------|-------------|-------------|------|
| 查询指定申报的验收申请记录 | GET | `/project/acceptance/my/{applicationId}` | — | 验收申请记录摘要（id、acceptanceStatus）或 null | 用于入口资格校验 |

### 1.5 非功能需求

- **权限**：企业端已认证用户，且为该申报的申报人（`applicant_user_id` 匹配）方可发起验收申请。

### 1.6 验收标准

- [ ] 申报状态为 `COUNTY_APPROVED` 的申报详情页底部展示"发起验收申请"按钮。
- [ ] 点击按钮时查询验收申请状态，无记录时跳转至验收填报页。
- [ ] 已有进行中验收申请时，按钮变为"查看验收进度"，跳转至验收进度页。
- [ ] 验收已通过时，展示"验收已通过"标签，无按钮。
- [ ] 非 `COUNTY_APPROVED` 状态的申报详情页不展示任何验收相关按钮。
- [ ] 见可交互 HTML 原型。

---

## 功能二：竣工投产前验收申请信息填报

### 2.1 基本信息

- **功能名称**：竣工投产前验收申请信息填报
- **关联现有模块/功能**：
  - 用户认证：复用现有 `/login`、`/getInfo`、`/getRouters` 接口与 JWT 机制
  - 畜牧项目建设申报：复用 `project_application` 及 `project_application_item` 表中的申报填报数据（大部分字段自动回显）
  - 文件上传：复用现有文件上传接口（建设内容照片上传）
  - 功能一：验收申请入口，填报页通过 `application_id` 关联具体申报
  - 参考文档：`doc_spec/现有业务说明.spec.md`
- **目标用户/场景**：

| 用户角色 | 使用场景 | 主要需求 |
|---------|---------|---------|
| 养殖场申报主体（企业端用户） | 完成项目建设后，填写竣工投产前验收申请书并提交 | 确认回显的申报信息无误，补充填写建设完成情况，上传建设内容照片（建设前/中/后），提交等待审核 |

### 2.2 目标与范围

**目标**：
1. 提供与《新/改/扩建项目竣工投产前验收申请书》格式对应的线上填报表单。
2. 绝大多数字段直接从关联申报记录（`project_application`）自动回显（只读），无需用户重复填写。
3. 用户需要额外补充填写：申请时间（系统自动生成）、建设内容照片上传。
4. 建设内容照片按建设项目分类上传，支持"建设前"、"建设中"、"建设后"三个阶段的照片。
5. 支持"暂存草稿"功能，提交后状态变为"待乡镇畜牧兽医站核实"，进入审核流程。

**非目标（Non-Goals）**：
- ❌ 不支持修改从申报记录回显的信息（如需修改须回到申报流程，验收阶段不开放编辑）。
- ❌ 不支持批量导入照片数据。
- ❌ 已提交后不允许编辑（仅可在"被退回"状态下修改照片后重新提交）。

### 2.3 现状与复用

**现状简述**：
- 验收申请填报页为新增页面，大量字段从 `project_application` 表自动回显，用户核心操作为上传建设内容照片。
- 建设内容明细（`project_application_item`）直接展示，无需重新填写，用于照片分类参考。
- 文件上传使用现有系统的文件上传接口。

**复用检查结论**：

| 检查项 | 现有资源 | 复用结论 | 说明 |
|--------|---------|---------|------|
| 用户认证接口 | `/login`、`/getInfo`、`/getRouters` | ✅ 直接复用 | 见现有业务说明.spec.md 二、认证与通用 |
| 文件上传接口 | 现有文件上传接口 | ✅ 直接复用 | 建设内容照片上传复用通用上传接口 |
| 字典数据接口 | `/system/dict/data/type/{dictType}` | ✅ 直接复用 | 照片阶段类型等枚举值 |
| 申报主记录表 | `project_application`（申报PRD新建） | ✅ 直接复用 | 读取申报数据回显至验收表单，只读 |
| 申报建设内容明细表 | `project_application_item`（申报PRD新建） | ✅ 直接复用 | 读取建设内容明细，用于照片分类参考 |
| 验收申请主记录表 | `project_acceptance`（功能一新建） | ✅ 直接复用 | 功能一已定义，本功能写入数据 |
| 验收申请照片表 | 无 | 🆕 新建表 `project_acceptance_photo` | 存储建设内容照片 |
| 验收申请填报接口 | 无 | 🆕 新增 `/project/acceptance/draft`、`/project/acceptance/submit` | 无现有接口满足需求 |

### 2.4 功能需求

**功能列表**：

| 功能点 | 优先级 | 说明 |
|-------|-------|------|
| 申报信息自动回显 | P0 | 从关联申报记录自动回显全部基本信息字段（只读），用户无需重新填写 |
| 建设内容明细展示 | P0 | 展示申报时填写的建设内容明细表（只读），作为照片上传的参考依据 |
| 建设内容照片上传 | P0 | 按建设项目类别（圈舍、料塔、料线等）分类上传建设前/中/后照片 |
| 暂存草稿 | P1 | 点击"暂存"保存当前上传的照片，不进行完整校验，状态为"草稿" |
| 提交验收申请 | P0 | 点击"提交"执行校验（照片至少上传一张），通过后提交，状态变为"待核实" |
| 草稿继续填写 | P1 | 在功能三进度页找到草稿记录，点击"继续填写"进入本页面回显已上传照片 |
| 被退回后修改重提 | P1 | 审核退回后，可修改照片并重新提交 |

**关键流程**：

```
从功能一"发起验收申请"跳转（携带 application_id）
        ↓
进入验收申请填报页
        ↓
自动回显申报基本信息（只读）：申请单位、场址、联系人、联系电话、
养殖场类型、建设性质、建设期限、规模信息、资质办理情况、建设内容明细
        ↓
用户核实信息无误
        ↓
按建设内容分类上传照片（建设前/中/后）
        ↓
      ┌─────────────────────┐
      ↓                     ↓
   点击"暂存"             点击"提交"
 保存草稿（不校验）       执行校验（至少上传1张照片）
 状态：草稿               校验失败→提示错误
      ↓                     ↓
  回到申报列表页          校验通过→提交成功
                          状态：待核实
                          跳转至验收进度页
```

#### 2.4.1 功能流程逻辑

**验收申请状态流转**：
- `DRAFT`（草稿）：暂存后的状态，可继续编辑、上传照片、提交
- `PENDING_STATION_REVIEW`（待站核实）：企业提交后，等待乡镇畜牧兽医站核实
- `STATION_APPROVED`（站核实通过）：乡镇畜牧兽医站核实通过，等待乡镇政府审核
- `STATION_REJECTED`（站核实不通过）：乡镇畜牧兽医站核实不通过，已通知企业
- `PENDING_TOWN_REVIEW`（待镇审核）：乡镇畜牧兽医站通过后，等待乡镇政府审核
- `TOWN_APPROVED`（镇审核通过）：乡镇政府审核通过，等待县农业农村局审核
- `TOWN_REJECTED`（镇审核不通过）：乡镇政府审核不通过，已通知企业
- `PENDING_COUNTY_REVIEW`（待县审核）：乡镇政府通过后，等待县农业农村局审核
- `COUNTY_APPROVED`（县审核通过）：县农业农村局审核通过，验收申请完结
- `COUNTY_REJECTED`（县审核不通过）：县农业农村局审核不通过，已通知企业
- `WITHDRAWN`（已撤回）：企业端用户主动撤回草稿状态的验收申请

**被退回后重新提交规则**：
- 仅 `STATION_REJECTED`、`TOWN_REJECTED`、`COUNTY_REJECTED` 状态下允许企业端用户进入编辑页面修改内容（补充或替换照片）。
- 修改并重新提交后，状态重置为 `PENDING_STATION_REVIEW`，重新进入审核流程。

**照片上传分类规则**：
- 照片上传区按以下固定的建设项目类别分类展示上传组（类别与申报表中的建设内容子项目对应）：
  1. 圈舍建设
  2. 自动化饲喂系统建设
  3. 智能化管理系统建设
  4. 防疫系统建设
  5. 粪污处理系统建设
  6. 智能化环控系统建设
- 每个类别下设三个子上传区，分别对应照片阶段：建设前、建设中、建设后。
- 仅展示本次申报中实际填写了建设内容明细的子项目类别（即在 `project_application_item` 中存在对应 `sub_project` 的类别），其余类别不显示。

#### 2.4.2 数据模型与字段定义

**核心实体一：验收申请主记录表（`project_acceptance`）**——见功能一1.4.2，补充以下字段：

| 字段名（中文） | 字段名（英文） | 类型 | 长度 | 必填 | 默认值 | 取值范围/枚举值 | 说明 |
|-------------|-------------|------|------|------|-------|--------------|------|
| 申报单位意见 | applicant_opinion | varchar | 500 | 否 | NULL | 最大长度500字符 | 申报单位签字意见文字描述，选填 |
| 审核退回意见 | reject_reason | varchar | 500 | 否 | NULL | 最大长度500字符 | 审核不通过时记录退回原因 |
| PDF文件路径 | pdf_file_path | varchar | 500 | 否 | NULL | 最大长度500字符 | PDF生成后存储路径，NULL表示未生成 |
| PDF生成时间 | pdf_generate_time | datetime | — | 否 | NULL | — | PDF最近一次生成的时间 |

**核心实体二：验收申请照片表（`project_acceptance_photo`）**

| 字段名（中文） | 字段名（英文） | 类型 | 长度 | 必填 | 默认值 | 取值范围/枚举值 | 说明 |
|-------------|-------------|------|------|------|-------|--------------|------|
| 照片ID | id | bigint | — | 是 | 自增 | — | 主键 |
| 验收申请ID | acceptance_id | bigint | — | 是 | — | — | 外键，关联 `project_acceptance.id` |
| 建设项目类别 | sub_project | varchar | 50 | 是 | — | `PIGSTY`圈舍建设、`AUTO_FEEDING`自动化饲喂系统建设、`SMART_MGMT`智能化管理系统建设、`EPIDEMIC_PREVENTION`防疫系统建设、`MANURE_TREATMENT`粪污处理系统建设、`SMART_ENV_CTRL`智能化环控系统建设（仅此6种取值） | 与申报表子项目枚举值保持一致 |
| 照片阶段 | photo_stage | varchar | 20 | 是 | — | `BEFORE`建设前、`DURING`建设中、`AFTER`建设后（仅此3种取值） | — |
| 文件名称 | file_name | varchar | 200 | 是 | — | 最大长度200字符 | 原始文件名 |
| 文件路径 | file_path | varchar | 500 | 是 | — | 最大长度500字符 | 服务器存储路径或OSS路径 |
| 文件大小（字节） | file_size | bigint | — | 否 | NULL | 最大值52428800（50MB） | — |
| 排序序号 | sort_order | int | — | 是 | — | 最小值1，最大值999 | 同一类别+阶段内的照片排序 |
| 创建时间 | create_time | datetime | — | 是 | 当前时间 | — | 自动生成，只读 |

#### 2.4.3 界面交互逻辑

**页面布局**：
- 页面顶部展示面包屑：项目管理 > 我的申报 > 竣工验收申请填报。
- 表单区域按以下分区布局（与纸质申请书对应）：
  1. **申报信息回显区**（整体只读，顶部加灰色背景区分）：
     - 申请单位名称（从 `project_application.company_name` 回显）
     - 申请时间（系统当前日期自动生成，格式：`YYYY年MM月DD日`）
     - 场址（镇/村/社）
     - 养殖场类型
     - 联系人
     - 联系电话
     - 建设性质
     - 建设期限
     - 设计存栏规模（头）
     - 设计年出栏规模（头）
     - 建成后新增存栏（头）
     - 建成后新增出栏（头）
     - 建成后饲养量（头）
     - 现存栏（头）
     - 养殖场资质办理情况（列出上传的6类附件名称）
  2. **建设内容明细展示区**（只读表格）：展示申报时填写的全部建设内容明细行（子项目、建设内容、单位、数量、单价、金额、型号、备注），最后一行展示合计投资金额。
  3. **建设内容照片上传区**（核心可操作区域）：
     - 按实际申报中存在的子项目类别逐一展示上传分组
     - 每个子项目类别下分为三个子区域：建设前照片、建设中照片、建设后照片
     - 每个子区域展示：照片缩略图列表（含删除按钮）+ 上传按钮
  4. **申报单位意见区**：文本域输入框（选填，最大500字符）
  5. **底部操作区**：左侧"暂存"按钮（次要按钮），右侧"提交"按钮（主色按钮）

**照片上传交互**：
- 支持的文件格式：JPG、JPEG、PNG（不支持其他格式，上传时前端过滤并提示"仅支持JPG、PNG图片格式"）。
- 单张照片大小限制：最大 50MB，超出时提示"单张照片大小不能超过50MB"。
- 每个子项目类别下每个阶段最多上传 10 张照片，超过时"上传"按钮隐藏，提示"该阶段照片最多上传10张"。
- 照片上传成功后在对应区域显示缩略图，点击缩略图可放大预览，点击删除按钮可移除。
- 各子项目类别和各阶段均为独立上传区，互不影响。
- 顶部提示文字（蓝色信息框）："请上传圈舍、料塔、料线、饮水器等建设前、建设中、建设后的实景照片，至少上传1张照片。"

**已回显信息区的交互说明**：
- 所有回显字段使用灰色背景只读输入框展示（视觉上与可编辑字段明显区分）。
- 区域顶部展示提示文字："以下信息来自您的项目申报，不可修改。"（蓝色信息框）。
- 建设内容明细表以只读表格形式展示，不可添加/修改/删除。

**被退回后进入填报页**：
- 表单顶部展示退回意见提示框（橙色背景），内容为最新一次审核不通过的退回原因。
- 仅照片上传区域可重新操作（增删照片），其余字段仍为只读。
- 底部操作区展示"重新提交"按钮（主色按钮），不展示"暂存"按钮。

**校验规则（提交时触发，暂存时不触发）**：
- 照片上传：全部实际申报子项目中，至少有一张照片（任意类别任意阶段均可），未上传任何照片时提示"请至少上传1张建设内容照片"。
- 校验不通过时，页面自动滚动至照片上传区域，对应错误提示展示在照片上传区顶部（红色字体）。

#### 2.4.4 接口路径定义

| 接口名称 | HTTP 方法 | 路径 | 请求参数摘要 | 响应数据摘要 | 备注 |
|----------|-----------|------|-------------|-------------|------|
| 查询关联申报详情（含明细） | GET | `/project/application/{id}` | — | 申报完整信息+明细行+附件 | 复用申报PRD已定义接口，用于回显 |
| 暂存验收申请草稿 | POST | `/project/acceptance/draft` | application_id、applicantOpinion、照片数组（acceptanceId可选） | 操作结果+验收申请ID | 不执行完整校验 |
| 提交验收申请 | POST | `/project/acceptance/submit` | 同上 | 操作结果+验收申请ID | 执行完整校验，状态变为 `PENDING_STATION_REVIEW` |
| 更新草稿/重新提交 | PUT | `/project/acceptance` | 同上+验收申请ID | 操作结果 | 草稿更新或被退回后重新提交 |
| 查询验收申请详情 | GET | `/project/acceptance/{id}` | — | 验收申请完整信息（含照片列表） | — |
| 上传建设内容照片 | POST | `/project/acceptance/photo/upload` | multipart form data（file + acceptanceId + subProject + photoStage） | 照片ID+文件路径 | 复用通用上传能力 |
| 删除建设内容照片 | DELETE | `/project/acceptance/photo/{id}` | — | 操作结果 | 草稿/退回状态下可删除 |
| 撤回验收申请 | PUT | `/project/acceptance/withdraw/{id}` | — | 操作结果 | 草稿状态可撤回，状态变为 `WITHDRAWN` |

### 2.5 非功能需求

- **权限**：仅企业端已认证用户（申报人本人）可操作，验收申请记录仅归属用户本人可见（数据隔离）。
- **兼容性**：桌面端主流分辨率（1280px 及以上）下表单布局正常。

### 2.6 验收标准

- [ ] 从"发起验收申请"按钮跳转后，申报基本信息和建设内容明细全部自动回显，字段均为只读状态。
- [ ] 照片上传区按实际申报的子项目类别展示分组（未申报的类别不显示），每组有建设前/中/后三个子区域。
- [ ] 上传照片成功后显示缩略图，点击可放大预览，可删除。
- [ ] 点击"暂存"保存草稿，不校验，可在进度页继续填写。
- [ ] 点击"提交"时，若未上传任何照片则提示"请至少上传1张建设内容照片"，有照片时提交成功，状态变为"待核实"，跳转至验收进度页。
- [ ] 被审核退回后，填报页顶部显示退回原因，照片可重新上传，点击"重新提交"可再次提交。
- [ ] 见可交互 HTML 原型。

---

## 功能三：验收申请进度跟踪与结果查看

### 3.1 基本信息

- **功能名称**：验收申请进度跟踪与结果查看
- **关联现有模块/功能**：
  - 用户认证：复用现有 `/login`、`/getInfo`、`/getRouters` 接口与 JWT 机制
  - 站内消息：审核不通过时推送消息
  - 功能二：验收申请信息填报（草稿继续填写、退回后修改入口）
  - 参考文档：`doc_spec/现有业务说明.spec.md`
- **目标用户/场景**：

| 用户角色 | 使用场景 | 主要需求 |
|---------|---------|---------|
| 养殖场申报主体（企业端用户） | 提交验收申请后跟踪审核进度 | 查看验收申请当前状态、历史流程节点进度、审核意见，被退回时及时知晓并修改重提 |

### 3.2 目标与范围

**目标**：
1. 提供当前用户所有验收申请记录的列表视图。
2. 提供单条验收申请的详情页，展示完整流程进度（四个节点：填报提交/乡镇畜牧兽医站核实/乡镇政府审核/县农业农村局审核）、已填报信息摘要、上传的照片。
3. 审核不通过时，在详情页明确展示退回原因，并提供"修改并重新提交"入口。
4. 草稿状态提供"继续填写"入口和"撤回"入口。
5. 县农业农村局审核通过后，提供PDF验收申请书下载入口。

**非目标（Non-Goals）**：
- ❌ 不支持对已提交（非草稿、非退回）验收申请进行编辑。
- ❌ 不提供验收数据统计分析功能。

### 3.3 现状与复用

**现状简述**：
- 验收申请进度页为新增页面，数据来源于 `project_acceptance` 表（主要只读展示）。
- 审核节点记录表复用申报流程中新建的 `project_application_audit_log` 结构，新建 `project_acceptance_audit_log` 表存储验收审核记录。

**复用检查结论**：

| 检查项 | 现有资源 | 复用结论 | 说明 |
|--------|---------|---------|------|
| 用户认证接口 | `/login`、`/getInfo`、`/getRouters` | ✅ 直接复用 | 见现有业务说明.spec.md 二、认证与通用 |
| 验收申请主记录表 | `project_acceptance`（功能一/二新建） | ✅ 直接复用 | 本功能只读查询 |
| 验收申请照片表 | `project_acceptance_photo`（功能二新建） | ✅ 直接复用 | 本功能展示照片 |
| 验收审核日志表 | 无 | 🆕 新建表 `project_acceptance_audit_log` | 参照申报PRD中 `project_application_audit_log` 结构新建 |
| 验收申请列表接口 | 无 | 🆕 新增 `/project/acceptance/my/list` | 按当前登录用户查询验收申请列表 |
| 验收申请详情接口 | `/project/acceptance/{id}`（功能二新增） | ✅ 直接复用 | 功能二已新增，本功能复用，扩展返回审核日志 |
| PDF下载接口 | 无 | 🆕 新增 `/project/acceptance/{id}/pdf` | — |

### 3.4 功能需求

**功能列表**：

| 功能点 | 优先级 | 说明 |
|-------|-------|------|
| 我的验收申请列表 | P0 | 展示当前用户所有验收申请记录，含项目名称、申请时间、当前状态、操作按钮 |
| 状态筛选Tab | P1 | 全部/草稿/审核中/已通过/已退回，切换立即筛选 |
| 验收申请详情查看 | P0 | 展示完整申报回显信息、建设内容照片、流程进度轴、各审核节点意见 |
| 流程进度展示 | P0 | 步骤条展示4个审核节点的当前状态（待处理/进行中/已通过/已退回） |
| 审核不通过通知 | P0 | 站内消息通知企业端用户审核不通过，消息内含退回原因和跳转链接 |
| 草稿继续填写 | P1 | 草稿状态展示"继续填写"按钮，跳转至功能二填报页 |
| 草稿撤回 | P1 | 草稿状态展示"撤回"按钮，确认后状态变为 `WITHDRAWN` |
| 退回后修改重提 | P0 | 任意退回状态展示"修改并重新提交"按钮，跳转至功能二填报页 |
| PDF验收申请书下载 | P1 | 县审核通过后展示"下载验收申请书PDF"按钮 |

**关键流程**：

```
进入我的验收申请列表页
        ↓
展示所有验收申请记录（按创建时间倒序）
        ↓
点击记录 → 进入验收申请详情页
        ↓
查看申报回显信息 + 照片 + 审核流程进度轴
        ↓
  根据当前状态展示操作：
  - 草稿：继续填写 / 撤回
  - 审核中：无操作按钮，仅查看
  - 任意退回：修改并重新提交（展示退回原因）
  - 县审核通过：下载PDF
```

#### 3.4.1 功能流程逻辑

**流程进度轴节点定义**（4个节点）：

| 步骤序号 | 节点名称 | 对应状态区间 | 展示逻辑 |
|---------|---------|------------|---------|
| 步骤1 | 填报提交 | 任意非 `DRAFT` 状态均为已完成 | 完成态（绿色勾） |
| 步骤2 | 乡镇畜牧兽医站核实 | `PENDING_STATION_REVIEW` 为进行中；`STATION_APPROVED` 为已完成；`STATION_REJECTED` 为退回（红色叉） | — |
| 步骤3 | 乡镇政府审核 | `PENDING_TOWN_REVIEW` 为进行中；`TOWN_APPROVED` 为已完成；`TOWN_REJECTED` 为退回（红色叉）；`STATION_APPROVED` 之前为未开始（灰色） | — |
| 步骤4 | 县农业农村局审核 | `PENDING_COUNTY_REVIEW` 为进行中；`COUNTY_APPROVED` 为已完成（绿色勾）；`COUNTY_REJECTED` 为退回（红色叉）；`TOWN_APPROVED` 之前为未开始（灰色） | — |

**状态分组（用于列表Tab筛选）**：
- **草稿**：`DRAFT`
- **审核中**：`PENDING_STATION_REVIEW`、`STATION_APPROVED`、`PENDING_TOWN_REVIEW`、`TOWN_APPROVED`、`PENDING_COUNTY_REVIEW`
- **已通过**：`COUNTY_APPROVED`
- **已退回**：`STATION_REJECTED`、`TOWN_REJECTED`、`COUNTY_REJECTED`

**站内消息通知规则**：
- 触发时机：乡镇畜牧兽医站、乡镇政府、县农业农村局任意一方审核不通过时，系统自动推送站内消息。
- 消息内容模板：「您的【{项目名称}】项目竣工投产前验收申请已被{审核机构名称}退回，退回原因：{退回意见}。请点击查看详情并修改重新提交。」
- 消息跳转：点击消息跳转至对应验收申请详情页。

#### 3.4.2 数据模型与字段定义

**核心实体：验收审核流程节点记录表（`project_acceptance_audit_log`）**

| 字段名（中文） | 字段名（英文） | 类型 | 长度 | 必填 | 默认值 | 取值范围/枚举值 | 说明 |
|-------------|-------------|------|------|------|-------|--------------|------|
| 日志ID | id | bigint | — | 是 | 自增 | — | 主键 |
| 验收申请ID | acceptance_id | bigint | — | 是 | — | — | 外键，关联 `project_acceptance.id` |
| 审核节点 | audit_node | varchar | 30 | 是 | — | `STATION_AUDIT`站核实、`TOWN_AUDIT`镇政府审核、`COUNTY_AUDIT`县农业农村局审核（仅此3种取值） | — |
| 审核结果 | audit_result | varchar | 20 | 是 | — | `APPROVED`通过、`REJECTED`不通过（仅此2种取值） | — |
| 审核意见 | audit_opinion | varchar | 500 | 否 | NULL | 最大长度500字符 | 退回原因或通过意见 |
| 审核人ID | auditor_user_id | bigint | — | 是 | — | — | 操作人员用户ID |
| 审核人姓名 | auditor_name | varchar | 50 | 是 | — | 最大长度50字符 | — |
| 审核时间 | audit_time | datetime | — | 是 | 当前时间 | — | 自动记录 |
| 创建时间 | create_time | datetime | — | 是 | 当前时间 | — | 自动生成，只读 |

#### 3.4.3 界面交互逻辑

**我的验收申请列表页**：
- 页面路径：项目管理 > 我的验收申请。
- 状态 Tab：全部 / 草稿 / 审核中 / 已通过 / 已退回，默认激活"全部"Tab，切换立即筛选。
- 申请记录卡片：项目名称（加粗，来自关联申报的 `project_notice.project_name`）、申请时间（`YYYY-MM-DD`）、当前状态标签（草稿-灰色、审核中-蓝色、已通过-绿色、已退回-红色）、快捷操作按钮（草稿显示"继续填写"，退回显示"修改重提"）。
- 分页，每页展示 10 条，按创建时间倒序。

**验收申请详情页**：
- 面包屑：项目管理 > 我的验收申请 > 验收申请详情。
- **信息摘要区**：展示申请单位、申请时间、项目名称、场址等关键字段（只读）。
- **审核流程进度轴**：横向步骤条，4个节点，展示逻辑同申报进度轴（见3.4.1）；退回节点下方展示退回意见（红色字体）。
- **建设内容照片区**：折叠展示，默认折叠；展开后按子项目类别+阶段分组显示照片缩略图，点击放大预览。
- **申报回显内容折叠区**：默认折叠，展开后查看完整申报信息（只读，与填报页回显内容一致）。
- **底部操作区**：根据状态展示对应按钮（见3.4功能列表）。
- 退回状态下，页面顶部展示橙色提示框，内容为最新一次退回原因。

**校验规则**：
- 点击"撤回"时弹出确认弹窗："确认撤回该验收申请草稿吗？撤回后将无法恢复。"，确认后执行撤回，取消则关闭弹窗。

#### 3.4.4 接口路径定义

| 接口名称 | HTTP 方法 | 路径 | 请求参数摘要 | 响应数据摘要 | 备注 |
|----------|-----------|------|-------------|-------------|------|
| 查询我的验收申请列表 | GET | `/project/acceptance/my/list` | pageNum、pageSize、statusGroup（DRAFT/IN_REVIEW/APPROVED/REJECTED/ALL） | 分页列表+总数 | 仅返回当前登录用户的记录 |
| 查询验收申请详情 | GET | `/project/acceptance/{id}` | — | 验收完整信息+照片列表+审核日志 | 🔧 改造复用功能二接口，新增审核日志数组 |
| 撤回验收申请草稿 | PUT | `/project/acceptance/withdraw/{id}` | — | 操作结果 | 功能二已定义，直接复用 |
| 下载验收申请书PDF | GET | `/project/acceptance/{id}/pdf` | — | PDF文件流 | 仅 `COUNTY_APPROVED` 状态可用 |

### 3.5 非功能需求

- **安全与权限**：验收申请记录严格按用户ID做数据隔离，用户只能查看自己的验收申请，越权访问返回 403。
- **消息通知**：审核不通过消息须在审核操作完成后 5 秒内推送至企业端用户。

### 3.6 验收标准

- [ ] 企业端用户在"项目管理 > 我的验收申请"菜单下能看到验收申请记录列表。
- [ ] 状态 Tab 切换即时筛选，草稿/审核中/已通过/已退回分组正确。
- [ ] 验收申请详情页流程进度轴正确展示4个节点的当前状态，退回节点显示退回意见。
- [ ] 建设内容照片折叠区可展开，照片按类别+阶段分组展示，支持点击放大预览。
- [ ] 草稿状态记录点击"继续填写"跳转至填报页，已上传照片正确回显。
- [ ] 任意退回状态下，详情页顶部展示退回原因，可点击"修改并重新提交"进入填报页。
- [ ] 审核不通过时，消息中心收到系统通知，通知内含退回原因和跳转链接。
- [ ] 县审核通过后，详情页展示"下载验收申请书PDF"按钮，点击可下载PDF文件。
- [ ] 见可交互 HTML 原型。

---

## 附录

### 验收申请与申报表字段对照

| 纸质申请书字段 | 数据来源 | 所属表 | 是否由用户新填 |
|-------------|--------|-------|------------|
| 申请单位（营业执照注册全名） | `company_name` | `project_application` | 否（回显） |
| 申请时间 | 系统当前日期自动生成 | `project_acceptance.apply_date` | 否（自动） |
| 场址 | `address_town/village/group` | `project_application` | 否（回显） |
| 养殖场类型 | `farm_type` | `project_application` | 否（回显） |
| 联系人 | `contact_name` | `project_application` | 否（回显） |
| 联系电话 | `contact_phone` | `project_application` | 否（回显） |
| 建设性质 | `construction_type` | `project_application` | 否（回显） |
| 建设期限 | `build_start_date/end_date` | `project_application` | 否（回显） |
| 设计存栏规模（头） | `design_stock` | `project_application` | 否（回显） |
| 设计年出栏规模（头） | `design_annual_output` | `project_application` | 否（回显） |
| 建成后新增存栏（头） | `new_stock_after_build` | `project_application` | 否（回显） |
| 建成后新增出栏（头） | `new_output_after_build` | `project_application` | 否（回显） |
| 建成后饲养量（头） | `total_raise_after_build` | `project_application` | 否（回显，自动计算） |
| 现存栏（头） | `current_stock` | `project_application` | 否（回显） |
| 养殖场资质办理情况 | 附件列表文字 | `project_application_attachment` | 否（回显） |
| 建设内容明细 | 全部明细行 | `project_application_item` | 否（回显） |
| 合计投资金额 | `total_investment` | `project_application` | 否（回显） |
| 建设内容照片 | 照片文件 | `project_acceptance_photo` | **是（用户新上传）** |
| 申报单位意见 | `applicant_opinion` | `project_acceptance` | 是（选填） |
| 畜牧兽医站意见 | `audit_opinion`（STATION_AUDIT节点） | `project_acceptance_audit_log` | 否（审核时填写） |
| 乡镇人民政府意见 | `audit_opinion`（TOWN_AUDIT节点） | `project_acceptance_audit_log` | 否（审核时填写） |
