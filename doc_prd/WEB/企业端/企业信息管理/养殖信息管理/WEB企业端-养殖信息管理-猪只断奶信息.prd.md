# WEB企业端-养殖信息管理-猪只断奶信息

> **文档版本**：v2.0
> **创建时间**：2026-03-04
> **更新时间**：2026-03-05
> **文档状态**：待评审

---

## 3.1 基本信息

- **功能名称**：猪只断奶信息管理
- **关联现有模块/功能**：
  - 用户认证：依赖现有 JWT Token 认证（`/login`、`/getInfo`）
  - 繁育信息：断奶记录需关联 `pig_breeding_record`（繁育信息表），通过母猪耳号关联
  - 圈舍信息：断奶后转入圈舍依赖 `pig_pigsty`（圈舍表）
  - 文件上传：复用现有 `/common/upload` 接口
  - 参考文档：`doc_spec/现有业务说明.spec.md`
- **目标用户/场景**：

| 角色名称 | 角色编码 | 使用场景 | 主要操作 |
|---------|---------|---------|---------|
| 企业管理员 | ent_admin | 管理本企业数据上报 | 新增、编辑、删除草稿、提交审核、撤回 |
| 企业填报员 | ent_reporter | 日常数据填报 | 新增、编辑、删除草稿、提交审核、撤回 |
| 乡镇畜牧兽医站 | town_vet_station | 对企业上报数据进行技术核实 | 查看、核实通过、核实退回 |
| 乡镇政府业务人员 | town_gov_staff | 对核实通过数据进行行政审核 | 查看、审核通过、审核退回 |
| 县级管理员 | county_admin | 管理全县数据，可直接新增 | 新增（直接入库）、编辑、删除、查看全部 |
| 县级业务人员 | county_staff | 最终审核确认入库 | 查看、终审通过入库、终审退回 |

---

## 3.2 目标与范围

**目标**：
1. 为养殖企业用户提供WEB端猪只断奶信息填报入口，关联繁育记录追踪仔猪断奶情况。
2. 校验断奶数量不超过对应胎次活仔数，确保数据准确性。
3. 实现四级审批流程：企业上报→乡镇畜牧兽医站核实→乡镇政府审核→县级审核。
4. 支持数据的新增、草稿保存、提交审核、撤回、修改、查询、导出Excel。
5. 所有角色在同一界面操作，通过操作列按钮区分职责。
6. 县级管理员可直接新增数据（直接入库不走审核）、编辑/删除企业上报数据（留痕）。

**非目标（Non-Goals）**：
- ❌ 不做断奶统计报表。

---

## 3.3 现状与复用

**现状简述**：
- 小程序端已实现断奶信息上报功能，WEB端企业端需新增对应功能，逻辑与小程序端保持一致。
- 断奶数量业务规则：断奶数量不能大于对应繁育记录的活仔数（提交前校验）。
- 断奶日龄特殊提示规则：断奶日龄低于18天或高于35天时，需在备注中说明原因。

**复用检查结论**：

| 检查项 | 现有资源 | 复用结论 | 说明 |
|--------|---------|---------|------|
| 用户认证接口 | `/login`、`/getInfo` | ✅ 直接复用 | - |
| 文件上传接口 | `/common/upload` | ✅ 直接复用 | - |
| 繁育记录母猪下拉 | `/farm/breeding-records/sow-options` | ✅ 直接复用 | 见繁育信息PRD §3.4.4 |
| 圈舍下拉选项 | `/farm/pigsty/options` | ✅ 直接复用 | - |
| 断奶信息表 | `pig_weaning_record` | ✅ 直接复用 | 小程序端已定义 |
| 断奶信息接口 | 无WEB端接口 | 🆕 新增 | - |

---

## 3.4 功能需求

**功能列表**：

| 功能点 | 优先级 | 说明 |
|-------|-------|------|
| 新增断奶信息 | P0 | 弹窗表单，关联母猪繁育记录，支持草稿和提交 |
| 查询列表 | P0 | 分页查询，支持按企业名称、行政区划、状态、断奶日期范围、母猪耳号筛选 |
| 查看详情 | P0 | 查看完整填报内容及审核进度 |
| 编辑记录 | P0 | 草稿/退回状态可编辑 |
| 删除草稿 | P1 | 仅草稿状态可删除 |
| 撤回提交 | P1 | 待核实状态且乡镇畜牧兽医站未操作时可撤回 |
| 核实通过/退回 | P0 | 乡镇畜牧兽医站对待核实数据进行技术核实 |
| 审核通过/退回 | P0 | 乡镇政府对待审核数据进行行政审核 |
| 终审通过/退回 | P0 | 县级业务人员对待终审数据进行最终审核 |
| 导出Excel | P1 | 支持导出当前筛选结果或勾选数据 |

**关键流程**：

```
断奶信息填报关键流程：

企业用户新增断奶信息 → 弹窗表单
  → 选择母猪耳号（下拉，来自繁育记录）→ 系统自动回显：母猪耳号（只读）
  → 填写断奶日期、断奶日龄、断奶数量等
  → 填写断奶日龄时，若日龄 < 18 或 > 35：提示「断奶日龄异常，请在备注中说明原因」，备注变为必填
  → 提交时校验：断奶数量 ≤ 对应繁育记录活仔数
  → 提交确认弹窗：「提交后将进入审批流程：乡镇畜牧兽医站核实→乡镇政府审核→县级终审入库，提交前请确认信息无误。」
  → 确认后状态变为「待核实」

四级审批流程：
  企业提交 → 乡镇畜牧兽医站核实 → 乡镇政府审核 → 县级终审入库

县级管理员直接新增 → 状态直接为「已入库」，不走审批流程
```

**特殊业务规则**：
- 断奶日龄低于18天或高于35天时，提示警告，备注字段变为必填
- 断奶数量不能大于关联繁育记录的活仔数（提交前校验，提示「断奶数量不能超过该胎次活仔数X头」）

### 3.4.1 功能流程逻辑

**数据状态规则**（与所有填报模块一致）：

| 状态值 | 状态名称 | 显示颜色 | 说明 |
|--------|----------|---------|------|
| `0` | 草稿 | 灰色(#999999) | 企业已保存未提交 |
| `1` | 待核实 | 蓝色(#1890ff) | 等待乡镇畜牧兽医站核实 |
| `2` | 待审核 | 橙色(#fa8c16) | 等待乡镇政府审核 |
| `3` | 待终审 | 紫色(#722ed1) | 等待县级终审 |
| `4` | 已入库 | 绿色(#52c41a) | 终态 |
| `5` | 核实退回 | 红色(#f5222d) | 乡镇畜牧兽医站退回 |
| `6` | 审核退回 | 红色(#f5222d) | 乡镇政府退回 |
| `7` | 终审退回 | 红色(#f5222d) | 县级退回 |

**状态流转规则**：

```
草稿(0) → 待核实(1)               【企业提交】
待核实(1) → 待审核(2)              【乡镇畜牧兽医站核实通过】
待核实(1) → 核实退回(5)            【乡镇畜牧兽医站核实退回】
待核实(1) → 草稿(0)               【企业撤回（乡镇畜牧兽医站未操作前）】
待审核(2) → 待终审(3)              【乡镇政府审核通过】
待审核(2) → 审核退回(6)            【乡镇政府审核退回】
待终审(3) → 已入库(4)              【县级终审通过】
待终审(3) → 终审退回(7)            【县级终审退回】
核实退回(5) → 待核实(1)            【企业修改后重新提交】
审核退回(6) → 待核实(1)            【企业修改后重新提交】
终审退回(7) → 待核实(1)            【企业修改后重新提交】
```

**删除规则**：仅草稿状态（`status=0`）可删除。

**导出规则**：支持导出当前筛选结果或勾选数据。

**导出文件名**：`断奶信息_YYYYMMDD_HHmmss.xlsx`

### 3.4.2 数据模型与字段定义

**核心实体**：`pig_weaning_record`（断奶信息表）

**审核流程公共字段**：见 `WEB企业端-养殖信息管理-养殖信息.prd.md §3.4.2` 公共字段说明（v2.0 四级审批字段）。

**断奶信息表（pig_weaning_record）字段定义**：

| 字段名（中文） | 字段名（英文） | 类型 | 长度 | 必填 | 默认值 | 取值范围/枚举值 | 说明 |
|--------------|--------------|------|------|------|--------|----------------|------|
| 记录ID | record_id | bigint | 20 | 是 | 自增 | - | 主键，系统自动生成，只读 |
| 企业ID | enterprise_id | bigint | 20 | 是 | - | 关联 pig_enterprise.enterprise_id | 系统自动填充，只读 |
| 企业名称 | enterprise_name | varchar | 200 | 是 | - | - | 关联pig_enterprise自动回显，只读 |
| 行政区划 | area_name | varchar | 200 | 是 | - | - | 关联pig_enterprise所属行政区划自动回显，只读 |
| 繁育记录ID | breeding_record_id | bigint | 20 | 是 | - | 关联 pig_breeding_record.record_id | 选择母猪耳号后关联，只读 |
| 母猪耳号 | sow_ear_tag | varchar | 50 | 是 | - | 最大长度50字符 | 从繁育记录下拉选择后自动回显，只读 |
| 断奶日期 | weaning_date | date | - | 是 | 当前日期 | 不能大于当前日期 | - |
| 断奶日龄 | weaning_age | int | 11 | 是 | - | 最小值1，最大值60 | 单位：天；低于18或高于35须备注说明 |
| 断奶数量 | weaning_quantity | int | 11 | 是 | - | 最小值1，最大值30 | 不能大于关联胎次活仔数 |
| 断奶平均重 | avg_weaning_weight | decimal | 10,2 | 是 | - | 最小值0.01，最大值99.99 | 单位：公斤 |
| 转入圈舍ID | target_pigsty_id | bigint | 20 | 是 | - | 关联 pig_pigsty.pigsty_id | 断奶后转入的圈舍，下拉选择 |
| 操作人员 | operator_name | varchar | 50 | 否 | - | 最大长度50字符 | - |
| 照片附件 | photo_urls | varchar | 1000 | 否 | - | 多张用逗号分隔，最多9张 | jpg/jpeg/png，单张≤5MB |
| （审核公共字段） | - | - | - | - | - | - | 见公共字段说明 |
| 删除标志 | del_flag | char | 1 | 是 | `0` | `0` 存在、`2` 删除 | 仅2种 |
| 创建者 | create_by | varchar | 64 | 否 | - | - | 系统自动填充，只读 |
| 创建时间 | create_time | datetime | - | 是 | CURRENT_TIMESTAMP | - | 系统自动填充，只读 |
| 更新者 | update_by | varchar | 64 | 否 | - | - | 系统自动填充，只读 |
| 更新时间 | update_time | datetime | - | 是 | CURRENT_TIMESTAMP | - | 自动更新，只读 |
| 备注 | remark | varchar | 500 | 否 | - | 最大长度500字符 | 断奶日龄<18或>35时必填 |

### 3.4.3 界面交互逻辑

**列表页面布局**：
- 顶部筛选区：企业名称（文本输入，模糊搜索；企业端角色不显示）、行政区划（下拉级联选择；企业端角色不显示）、状态（下拉，8种新状态）、断奶日期范围（日期区间选择器）、母猪耳号（文本输入，模糊搜索）；右侧：「新增」（主色）、「导出Excel」
- 列表数据列：

| 列名 | 字段 | 宽度 | 说明 |
|-----|-----|------|------|
| 企业名称 | enterprise_name | 160px | 企业端角色不显示；监管端角色显示 |
| 行政区划 | area_name | 140px | 企业端角色不显示；监管端角色显示 |
| 母猪耳号 | sow_ear_tag | 110px | - |
| 断奶日期 | weaning_date | 110px | YYYY-MM-DD |
| 断奶日龄（天） | weaning_age | 110px | - |
| 断奶数量（头） | weaning_quantity | 110px | - |
| 断奶平均重（kg） | avg_weaning_weight | 130px | - |
| 转入圈舍 | 关联圈舍名称 | 120px | - |
| 状态 | status | 100px | 彩色标签 |
| 填报人 | create_by | 100px | - |
| 创建时间 | create_time | 160px | YYYY-MM-DD HH:mm |
| 操作 | - | 200px | 固定列 |

- 分页器：每页默认20条，支持切换10/20/50/100条
- 退回状态行整行标红色浅背景色（`#fff5f5`）

**操作列按角色显示规则**：

| 角色 | 草稿(0) | 待核实(1) | 待审核(2) | 待终审(3) | 已入库(4) | 核实退回(5) | 审核退回(6) | 终审退回(7) |
|------|---------|----------|----------|----------|----------|------------|------------|------------|
| 企业管理员/填报员 | 编辑、删除、提交 | 撤回、查看 | 查看 | 查看 | 查看 | 编辑、提交、查看 | 编辑、提交、查看 | 编辑、提交、查看 |
| 乡镇畜牧兽医站 | - | 核实通过、核实退回、查看 | 查看 | 查看 | 查看 | 查看 | 查看 | 查看 |
| 乡镇政府业务人员 | - | 查看 | 审核通过、审核退回、查看 | 查看 | 查看 | 查看 | 查看 | 查看 |
| 县级管理员 | 编辑、删除 | 查看 | 查看 | 查看 | 编辑、删除、查看 | 编辑、删除、查看 | 编辑、删除、查看 | 编辑、删除、查看 |
| 县级业务人员 | - | 查看 | 查看 | 终审通过、终审退回、查看 | 查看 | 查看 | 查看 | 查看 |

**新增/编辑弹窗表单**：
- 弹窗标题：新增时「新增断奶信息」，编辑时「编辑断奶信息」
- 弹窗宽度：600px；内容区最大高度：70vh
- 表单字段顺序（与数据模型一致）：
  1. 母猪耳号（必填，下拉选择，来自本企业繁育记录中的母猪耳号列表；选择后breeding_record_id自动关联，该字段只读）
  2. 断奶日期（必填，日期选择器，默认当前日期，不能大于当前日期）
  3. 断奶日龄（必填，数字输入，单位标注「天」，范围1-60）
  4. 断奶数量（必填，数字输入，单位标注「头」，范围1-30）
  5. 断奶平均重（必填，数字输入，单位标注「kg」，范围0.01-99.99）
  6. 转入圈舍（必填，下拉选择，来自本企业圈舍列表）
  7. 操作人员（选填，文本输入，最大50字符）
  8. 照片附件（选填，最多9张）
  9. 备注（选填，最大500字符；断奶日龄<18或>35时必填）
- 弹窗底部：「保存草稿」、「提交审核」、「取消」

**母猪耳号选择交互**：
- 下拉选项来自本企业繁育记录中有分娩记录的母猪耳号（active状态），显示格式：「母猪耳号（配种日期：YYYY-MM-DD）」
- 选择后，系统在下方只读区域回显关联繁育记录信息：配种日期、分娩日期、活仔数（仅供参考，提交时校验断奶数量不超过活仔数）

**断奶日龄异常提示**：
- 断奶日龄字段失焦时，若值 < 18 或 > 35，在字段下方显示橙色提示：「断奶日龄异常（低于18天或高于35天），请在备注中说明原因」；备注字段标为必填（`*`标注变红）
- 断奶日龄调整回18-35范围内时，橙色提示消失，备注字段恢复为选填

**前端校验规则（强制）**：

| 字段 | 校验规则 | 校验时机 | 提示文案 |
|-----|---------|---------|---------|
| 母猪耳号 | 必填（从下拉选择） | 提交时 | 「请选择母猪耳号」 |
| 断奶日期 | 必填，不能大于当前日期 | 失焦+提交时 | 「请选择断奶日期」/「断奶日期不能晚于今天」 |
| 断奶日龄 | 必填，整数，范围1-60 | 失焦+提交时 | 「断奶日龄不能为空」/「断奶日龄须在1～60之间」 |
| 断奶数量 | 必填，整数，范围1-30，且≤关联活仔数 | 失焦+提交时 | 「断奶数量不能为空」/「断奶数量须在1～30之间」/「断奶数量不能超过该胎次活仔数X头」 |
| 断奶平均重 | 必填，数值，范围0.01-99.99 | 失焦+提交时 | 「断奶平均重不能为空」/「断奶平均重须在0.01～99.99之间」 |
| 转入圈舍 | 必填 | 提交时 | 「请选择转入圈舍」 |
| 备注 | 断奶日龄<18或>35时必填，最大500字符 | 失焦+提交时 | 「请在备注中说明断奶日龄异常原因」 |

**提交确认弹窗**：
- 确认文案：「提交后将进入审批流程：乡镇畜牧兽医站核实→乡镇政府审核→县级终审入库，提交前请确认信息无误。」
- 展示摘要信息：上报类型（断奶信息）、母猪耳号、断奶日期、断奶数量

**查看详情弹窗**：
- 内容展示：关联母猪耳号、断奶日期、断奶日龄、断奶数量（头）、断奶平均重（kg）、转入圈舍、操作人员、照片附件、备注、填报人、创建时间
- 审批进度条（4步）：企业提交 → 乡镇畜牧兽医站核实 → 乡镇政府审核 → 县级终审入库
- 退回状态时在详情弹窗顶部显示退回原因

**导出Excel字段顺序**：企业名称、行政区划、母猪耳号、断奶日期、断奶日龄（天）、断奶数量（头）、断奶平均重（kg）、转入圈舍名称、操作人员、状态、填报人、创建时间、备注

### 3.4.4 接口路径定义（强制）

| 接口名称 | HTTP方法 | 路径 | 请求参数摘要 | 响应数据摘要 | 备注 |
|----------|---------|------|------------|------------|------|
| 分页查询断奶信息 | GET | `/farm/weaning-records/list` | pageNum、pageSize、enterpriseName、areaCode、sowEarTag、weaningDateStart、weaningDateEnd、status | 分页列表+总数 | 按角色数据权限隔离 |
| 查询断奶信息详情 | GET | `/farm/weaning-records/{id}` | id（路径参数） | 单条完整记录 | - |
| 新增断奶信息 | POST | `/farm/weaning-records` | 见3.4.2字段定义 | 操作结果 | - |
| 修改断奶信息 | PUT | `/farm/weaning-records` | 见3.4.2字段定义（含record_id） | 操作结果 | 仅草稿/退回状态 |
| 删除断奶信息 | DELETE | `/farm/weaning-records/{ids}` | ids（逗号分隔） | 操作结果 | 仅草稿状态 |
| 提交审核 | PUT | `/farm/weaning-records/submit` | id | 操作结果 | 状态变为待核实 |
| 撤回提交 | PUT | `/farm/weaning-records/revoke` | id | 操作结果 | 仅待核实且未被操作 |
| 核实通过 | PUT | `/farm/weaning-records/verify/pass` | id, opinion(选填) | 操作结果 | 乡镇畜牧兽医站操作 |
| 核实退回 | PUT | `/farm/weaning-records/verify/reject` | id, opinion(必填) | 操作结果 | 乡镇畜牧兽医站操作 |
| 审核通过 | PUT | `/farm/weaning-records/audit/pass` | id, opinion(选填) | 操作结果 | 乡镇政府操作 |
| 审核退回 | PUT | `/farm/weaning-records/audit/reject` | id, opinion(必填) | 操作结果 | 乡镇政府操作 |
| 终审通过 | PUT | `/farm/weaning-records/final/pass` | id, opinion(选填) | 操作结果 | 县级业务人员操作 |
| 终审退回 | PUT | `/farm/weaning-records/final/reject` | id, opinion(必填) | 操作结果 | 县级业务人员操作 |
| 操作日志查询 | GET | `/farm/weaning-records/{id}/logs` | id | 操作日志列表 | 返回该记录全部审批操作记录 |
| 导出Excel | POST | `/farm/weaning-records/export` | 筛选参数、ids（可选） | Excel文件流 | - |

---

## 3.5 非功能需求

- **性能**：列表首屏加载 < 2秒（P95）；表单提交 < 3秒（P95）；Excel导出 < 10秒
- **安全与权限**：
  - 企业角色（ent_admin、ent_reporter）：仅本企业数据
  - 乡镇角色（town_vet_station、town_gov_staff）：本乡镇所有企业数据
  - 县级角色（county_admin、county_staff）：全县所有数据
- **兼容性**：支持Chrome 90+、Edge 90+、Firefox 88+；最低分辨率1280×768

---

## 3.6 验收标准

- [ ] 列表正确显示断奶信息记录，按创建时间倒序排列
- [ ] 各角色按数据权限仅看到对应范围数据（企业仅本企业、乡镇仅本乡镇、县级看全部）
- [ ] 企业名称、行政区划列在企业端角色不显示，监管端角色显示
- [ ] 企业名称、行政区划筛选条件在企业端角色不显示，监管端角色显示
- [ ] 状态、断奶日期范围、母猪耳号筛选均生效
- [ ] 新增弹窗中母猪耳号下拉来自本企业繁育记录，选择后自动回显关联繁育记录信息
- [ ] 断奶日期默认填充当前日期，不能选择未来日期
- [ ] 断奶日龄输入<18或>35时，显示橙色提示，备注变为必填
- [ ] 提交时校验断奶数量不超过关联活仔数，违反时给出具体数量提示
- [ ] 保存草稿不校验必填，提交时全量校验
- [ ] 提交确认弹窗展示审批流程说明及上报类型（断奶信息）、母猪耳号、断奶日期、断奶数量
- [ ] 草稿/退回状态可编辑，其他状态只读
- [ ] 仅草稿状态可删除
- [ ] 操作列按角色正确显示对应操作按钮
- [ ] 乡镇畜牧兽医站可对待核实数据执行核实通过/核实退回
- [ ] 乡镇政府可对待审核数据执行审核通过/审核退回
- [ ] 县级业务人员可对待终审数据执行终审通过/终审退回
- [ ] 县级管理员可直接新增数据（直接入库）、编辑/删除企业上报数据
- [ ] 查看详情分区展示所有字段，退回状态显示退回原因
- [ ] 详情弹窗展示四步审批进度条：企业提交→乡镇畜牧兽医站核实→乡镇政府审核→县级终审入库
- [ ] 导出Excel包含所有规定字段（含企业名称、行政区划），状态列为中文
