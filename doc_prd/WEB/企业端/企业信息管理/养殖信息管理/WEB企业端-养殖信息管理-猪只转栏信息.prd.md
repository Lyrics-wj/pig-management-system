# WEB企业端-养殖信息管理-猪只转栏信息

> **文档版本**：v2.0
> **创建时间**：2026-03-04
> **更新时间**：2026-03-05
> **文档状态**：待评审

---

## 3.1 基本信息

- **功能名称**：猪只转栏信息管理
- **关联现有模块/功能**：
  - 用户认证：依赖现有 JWT Token 认证（`/login`、`/getInfo`）
  - 圈舍信息：依赖 `pig_pigsty`（圈舍表），源圈舍和目标圈舍均需选择
  - 文件上传：复用现有 `/common/upload` 接口
  - 数据字典：复用 `/system/dict/data/type/{dictType}` 接口（转栏原因）
  - 参考文档：`doc_spec/现有业务说明.spec.md`
- **目标用户/场景**：

| 角色名称 | 角色编码 | 使用场景 | 主要操作 |
|---------|---------|---------|---------|
| 企业管理员 | ent_admin | 管理本企业数据上报 | 新增、编辑、删除草稿、提交审核、撤回 |
| 企业填报员 | ent_reporter | 日常数据填报 | 新增、编辑、删除草稿、提交审核、撤回 |
| 乡镇畜牧兽医站 | town_vet_station | 对企业上报数据进行技术核实 | 查看、核实通过、核实退回 |
| 乡镇政府业务人员 | town_gov_staff | 对核实通过数据进行行政审核 | 查看、审核通过、审核退回 |
| 县级管理员 | county_admin | 管理全县数据，可直接新增 | 新增（直接入库）、编辑、删除、查看全部 |
| 县级业务人员 | county_staff | 最终审核确认入库 | 查看、终审通过入库、终审退回 |

---

## 3.2 目标与范围

**目标**：
1. 为养殖企业用户提供WEB端猪只转栏信息填报入口，记录圈舍间猪只转移情况。
2. 校验转栏数量不超过源圈舍当前存栏量，且目标圈舍不能与源圈舍相同。
3. 实现四级审批流程：企业上报→乡镇畜牧兽医站核实→乡镇政府审核→县级审核。
4. 支持数据的新增、草稿保存、提交审核、撤回、修改、查询、导出Excel。
5. 所有角色在同一界面操作，通过操作列按钮区分职责。
6. 县级管理员可直接新增数据（直接入库不走审核）、编辑/删除企业上报数据（留痕）。

**非目标（Non-Goals）**：
- ❌ 存栏量变更在记录「已入库」后才生效，本功能不直接修改圈舍存栏显示值。

---

## 3.3 现状与复用

**现状简述**：
- 小程序端已实现转栏信息上报功能，WEB端企业端需新增对应功能。
- 选择源圈舍后自动回显当前存栏量（只读），转栏数量不能超过该值。
- 目标圈舍不能与源圈舍相同。

**复用检查结论**：

| 检查项 | 现有资源 | 复用结论 | 说明 |
|--------|---------|---------|------|
| 用户认证接口 | `/login`、`/getInfo` | ✅ 直接复用 | - |
| 文件上传接口 | `/common/upload` | ✅ 直接复用 | - |
| 字典数据接口 | `/system/dict/data/type/{dictType}` | ✅ 直接复用 | 转栏原因 |
| 圈舍下拉选项 | `/farm/pigsty/options` | ✅ 直接复用 | 源圈舍和目标圈舍均使用 |
| 转栏信息表 | `pig_transfer_record` | ✅ 直接复用 | 小程序端已定义 |
| 转栏信息接口 | 无WEB端接口 | 🆕 新增 | - |

---

## 3.4 功能需求

**功能列表**：

| 功能点 | 优先级 | 说明 |
|-------|-------|------|
| 新增转栏信息 | P0 | 弹窗表单，选择源/目标圈舍，自动回显存栏量 |
| 查询列表 | P0 | 分页查询，支持按企业名称、行政区划、状态、转栏日期范围、源圈舍筛选 |
| 查看详情 | P0 | 查看完整填报内容及审核进度 |
| 编辑记录 | P0 | 草稿/退回状态可编辑 |
| 删除草稿 | P1 | 仅草稿状态可删除 |
| 撤回提交 | P1 | 待核实状态且乡镇畜牧兽医站未操作时可撤回 |
| 核实通过/退回 | P0 | 乡镇畜牧兽医站对待核实数据进行技术核实 |
| 审核通过/退回 | P0 | 乡镇政府对待审核数据进行行政审核 |
| 终审通过/退回 | P0 | 县级业务人员对待终审数据进行最终审核 |
| 导出Excel | P1 | 支持导出当前筛选结果或勾选数据 |

**关键流程**：

```
转栏信息填报关键流程：

企业用户新增转栏信息 → 弹窗表单
  → 选择源圈舍 → 系统自动回显：源圈舍当前存栏量（只读，显示：「当前存栏：X头」）
  → 选择目标圈舍 → 系统校验：目标圈舍不能与源圈舍相同（即时校验）
  → 填写转栏日期、转栏数量、转栏原因等
  → 提交时校验：转栏数量 ≤ 源圈舍当前存栏量
  → 提交确认弹窗：「提交后将进入审批流程：乡镇畜牧兽医站核实→乡镇政府审核→县级终审入库，提交前请确认信息无误。」
  → 确认后状态变为「待核实」

四级审批流程：
  企业提交 → 乡镇畜牧兽医站核实 → 乡镇政府审核 → 县级终审入库

县级管理员直接新增 → 状态直接为「已入库」，不走审批流程
```

### 3.4.1 功能流程逻辑

**数据状态规则**（与所有填报模块一致）：

| 状态值 | 状态名称 | 显示颜色 | 说明 |
|--------|----------|---------|------|
| `0` | 草稿 | 灰色(#999999) | 企业已保存未提交 |
| `1` | 待核实 | 蓝色(#1890ff) | 等待乡镇畜牧兽医站核实 |
| `2` | 待审核 | 橙色(#fa8c16) | 等待乡镇政府审核 |
| `3` | 待终审 | 紫色(#722ed1) | 等待县级终审 |
| `4` | 已入库 | 绿色(#52c41a) | 终态 |
| `5` | 核实退回 | 红色(#f5222d) | 乡镇畜牧兽医站退回 |
| `6` | 审核退回 | 红色(#f5222d) | 乡镇政府退回 |
| `7` | 终审退回 | 红色(#f5222d) | 县级退回 |

**状态流转规则**：

```
草稿(0) → 待核实(1)               【企业提交】
待核实(1) → 待审核(2)              【乡镇畜牧兽医站核实通过】
待核实(1) → 核实退回(5)            【乡镇畜牧兽医站核实退回】
待核实(1) → 草稿(0)               【企业撤回（乡镇畜牧兽医站未操作前）】
待审核(2) → 待终审(3)              【乡镇政府审核通过】
待审核(2) → 审核退回(6)            【乡镇政府审核退回】
待终审(3) → 已入库(4)              【县级终审通过】
待终审(3) → 终审退回(7)            【县级终审退回】
核实退回(5) → 待核实(1)            【企业修改后重新提交】
审核退回(6) → 待核实(1)            【企业修改后重新提交】
终审退回(7) → 待核实(1)            【企业修改后重新提交】
```

**删除规则**：仅草稿状态（`status=0`）可删除。

**导出规则**：支持导出当前筛选结果或勾选数据。

**导出文件名**：`转栏信息_YYYYMMDD_HHmmss.xlsx`

### 3.4.2 数据模型与字段定义

**核心实体**：`pig_transfer_record`（转栏信息表）

**审核流程公共字段**：见 `WEB企业端-养殖信息管理-养殖信息.prd.md §3.4.2` 公共字段说明（v2.0 四级审批字段）。

**转栏信息表（pig_transfer_record）字段定义**：

| 字段名（中文） | 字段名（英文） | 类型 | 长度 | 必填 | 默认值 | 取值范围/枚举值 | 说明 |
|--------------|--------------|------|------|------|--------|----------------|------|
| 记录ID | record_id | bigint | 20 | 是 | 自增 | - | 主键，系统自动生成，只读 |
| 企业ID | enterprise_id | bigint | 20 | 是 | - | 关联 pig_enterprise.enterprise_id | 系统自动填充，只读 |
| 企业名称 | enterprise_name | varchar | 200 | 是 | - | - | 关联pig_enterprise自动回显，只读 |
| 行政区划 | area_name | varchar | 200 | 是 | - | - | 关联pig_enterprise所属行政区划自动回显，只读 |
| 源圈舍ID | source_pigsty_id | bigint | 20 | 是 | - | 关联 pig_pigsty.pigsty_id | 下拉选择，选中后自动回显当前存栏量，只读 |
| 目标圈舍ID | target_pigsty_id | bigint | 20 | 是 | - | 关联 pig_pigsty.pigsty_id | 不能与源圈舍相同 |
| 转栏日期 | transfer_date | date | - | 是 | 当前日期 | 不能大于当前日期 | - |
| 转栏数量 | transfer_quantity | int | 11 | 是 | - | 最小值1，最大值9999 | 不能大于源圈舍当前存栏量 |
| 转栏原因 | transfer_reason | char | 1 | 是 | - | `1` 生长阶段转换、`2` 分群管理、`3` 治疗隔离、`4` 配种转群、`5` 其他 | 共5种，无其他 |
| 平均体重 | avg_weight | decimal | 10,2 | 否 | - | 最小值0.01，最大值999.99 | 单位：公斤 |
| 操作人员 | operator_name | varchar | 50 | 否 | - | 最大长度50字符 | - |
| 照片附件 | photo_urls | varchar | 1000 | 否 | - | 多张用逗号分隔，最多9张 | jpg/jpeg/png，单张≤5MB |
| （审核公共字段） | - | - | - | - | - | - | 见公共字段说明 |
| 删除标志 | del_flag | char | 1 | 是 | `0` | `0` 存在、`2` 删除 | 仅2种 |
| 创建者 | create_by | varchar | 64 | 否 | - | - | 系统自动填充，只读 |
| 创建时间 | create_time | datetime | - | 是 | CURRENT_TIMESTAMP | - | 系统自动填充，只读 |
| 更新者 | update_by | varchar | 64 | 否 | - | - | 系统自动填充，只读 |
| 更新时间 | update_time | datetime | - | 是 | CURRENT_TIMESTAMP | - | 自动更新，只读 |
| 备注 | remark | varchar | 500 | 否 | - | 最大长度500字符 | - |

### 3.4.3 界面交互逻辑

**列表页面布局**：
- 顶部筛选区：企业名称（文本输入，模糊搜索；企业端角色不显示）、行政区划（下拉级联选择；企业端角色不显示）、状态（下拉，8种新状态）、转栏日期范围（日期区间）、源圈舍（下拉）；右侧：「新增」（主色）、「导出Excel」
- 列表数据列：

| 列名 | 字段 | 宽度 | 说明 |
|-----|-----|------|------|
| 企业名称 | enterprise_name | 160px | 企业端角色不显示；监管端角色显示 |
| 行政区划 | area_name | 140px | 企业端角色不显示；监管端角色显示 |
| 源圈舍 | 关联源圈舍名称 | 120px | - |
| 目标圈舍 | 关联目标圈舍名称 | 120px | - |
| 转栏日期 | transfer_date | 110px | YYYY-MM-DD |
| 转栏数量（头） | transfer_quantity | 110px | - |
| 转栏原因 | transfer_reason | 120px | 枚举中文名 |
| 平均体重（kg） | avg_weight | 120px | 空值显示「—」 |
| 状态 | status | 100px | 彩色标签 |
| 填报人 | create_by | 100px | - |
| 创建时间 | create_time | 160px | YYYY-MM-DD HH:mm |
| 操作 | - | 200px | 固定列 |

- 分页：默认20条，支持10/20/50/100条切换

**操作列按角色显示规则**：

| 角色 | 草稿(0) | 待核实(1) | 待审核(2) | 待终审(3) | 已入库(4) | 核实退回(5) | 审核退回(6) | 终审退回(7) |
|------|---------|----------|----------|----------|----------|------------|------------|------------|
| 企业管理员/填报员 | 编辑、删除、提交 | 撤回、查看 | 查看 | 查看 | 查看 | 编辑、提交、查看 | 编辑、提交、查看 | 编辑、提交、查看 |
| 乡镇畜牧兽医站 | - | 核实通过、核实退回、查看 | 查看 | 查看 | 查看 | 查看 | 查看 | 查看 |
| 乡镇政府业务人员 | - | 查看 | 审核通过、审核退回、查看 | 查看 | 查看 | 查看 | 查看 | 查看 |
| 县级管理员 | 编辑、删除 | 查看 | 查看 | 查看 | 编辑、删除、查看 | 编辑、删除、查看 | 编辑、删除、查看 | 编辑、删除、查看 |
| 县级业务人员 | - | 查看 | 查看 | 终审通过、终审退回、查看 | 查看 | 查看 | 查看 | 查看 |

**新增/编辑弹窗表单**：
- 弹窗标题：「新增转栏信息」/「编辑转栏信息」
- 弹窗宽度：600px；内容区最大高度：70vh
- 表单字段顺序（与数据模型一致）：
  1. 源圈舍（必填，下拉选择；选中后下方以只读文本形式显示「当前存栏：X头」；当前存栏量不可编辑）
  2. 目标圈舍（必填，下拉选择；选中后即时校验与源圈舍是否相同，相同时在字段下方提示「目标圈舍不能与源圈舍相同」）
  3. 转栏日期（必填，日期选择器，默认当前日期）
  4. 转栏数量（必填，数字输入，单位：头，范围1-9999；字段旁提示当前存栏上限）
  5. 转栏原因（必填，下拉：生长阶段转换/分群管理/治疗隔离/配种转群/其他）
  6. 平均体重（选填，数值，单位：kg，范围0.01-999.99）
  7. 操作人员（选填，文本输入，最大50字符）
  8. 照片附件（选填，最多9张）
  9. 备注（选填，最大500字符）
- 弹窗底部：「保存草稿」、「提交审核」、「取消」

**源圈舍存栏量回显规则**：
- 源圈舍下拉选中后，立即调用接口获取该圈舍最新存栏量，以只读文本形式在下方显示「当前存栏：X头」（`background: #f5f5f5`，灰色背景，不可编辑）
- 编辑已有记录时，同样重新查询最新存栏量回显（不以填报时的快照为准）

**目标圈舍校验规则**：
- 选择目标圈舍后立即执行：若 target_pigsty_id = source_pigsty_id，则在目标圈舍字段下方显示红色错误提示「目标圈舍不能与源圈舍相同」，禁止提交

**前端校验规则（强制）**：

| 字段 | 校验规则 | 校验时机 | 提示文案 |
|-----|---------|---------|---------|
| 源圈舍 | 必填 | 提交时 | 「请选择源圈舍」 |
| 目标圈舍 | 必填，且不能与源圈舍相同 | 改变时+提交时 | 「请选择目标圈舍」/「目标圈舍不能与源圈舍相同」 |
| 转栏日期 | 必填，不能大于当前日期 | 失焦+提交时 | 「请选择转栏日期」/「转栏日期不能晚于今天」 |
| 转栏数量 | 必填，整数，范围1-9999，且≤源圈舍存栏量 | 失焦+提交时 | 「转栏数量不能为空」/「转栏数量须在1～9999之间」/「转栏数量不能超过源圈舍存栏量X头」 |
| 转栏原因 | 必填，枚举值1/2/3/4/5 | 提交时 | 「请选择转栏原因」 |
| 平均体重 | 选填，数值，范围0.01-999.99 | 失焦时 | 「平均体重须在0.01～999.99之间」 |

**提交确认弹窗**：
- 确认文案：「提交后将进入审批流程：乡镇畜牧兽医站核实→乡镇政府审核→县级终审入库，提交前请确认信息无误。」
- 展示摘要信息：上报类型（转栏信息）、源圈舍、目标圈舍、转栏日期

**查看详情弹窗**：
- 分区展示：转栏信息（源圈舍、目标圈舍、转栏日期、转栏数量）、其他信息（转栏原因、平均体重、操作人员、照片附件、备注、填报人、创建时间）
- 审批进度条（4步）：企业提交 → 乡镇畜牧兽医站核实 → 乡镇政府审核 → 县级终审入库
- 退回状态时在详情弹窗顶部显示退回原因

**导出Excel字段顺序**：企业名称、行政区划、源圈舍名称、目标圈舍名称、转栏日期、转栏数量（头）、转栏原因、平均体重（kg）、操作人员、状态、填报人、创建时间、备注

### 3.4.4 接口路径定义（强制）

| 接口名称 | HTTP方法 | 路径 | 请求参数摘要 | 响应数据摘要 | 备注 |
|----------|---------|------|------------|------------|------|
| 分页查询转栏信息 | GET | `/farm/transfer-records/list` | pageNum、pageSize、enterpriseName、areaCode、sourcePigstyId、transferDateStart、transferDateEnd、status | 分页列表+总数 | 按角色数据权限隔离 |
| 查询转栏信息详情 | GET | `/farm/transfer-records/{id}` | id | 单条完整记录 | - |
| 新增转栏信息 | POST | `/farm/transfer-records` | 见3.4.2字段定义 | 操作结果 | - |
| 修改转栏信息 | PUT | `/farm/transfer-records` | 见3.4.2字段定义（含record_id） | 操作结果 | 仅草稿/退回 |
| 删除转栏信息 | DELETE | `/farm/transfer-records/{ids}` | ids（逗号分隔） | 操作结果 | 仅草稿 |
| 提交审核 | PUT | `/farm/transfer-records/submit` | id | 操作结果 | 状态变为待核实 |
| 撤回提交 | PUT | `/farm/transfer-records/revoke` | id | 操作结果 | 仅待核实且未被操作 |
| 核实通过 | PUT | `/farm/transfer-records/verify/pass` | id, opinion(选填) | 操作结果 | 乡镇畜牧兽医站操作 |
| 核实退回 | PUT | `/farm/transfer-records/verify/reject` | id, opinion(必填) | 操作结果 | 乡镇畜牧兽医站操作 |
| 审核通过 | PUT | `/farm/transfer-records/audit/pass` | id, opinion(选填) | 操作结果 | 乡镇政府操作 |
| 审核退回 | PUT | `/farm/transfer-records/audit/reject` | id, opinion(必填) | 操作结果 | 乡镇政府操作 |
| 终审通过 | PUT | `/farm/transfer-records/final/pass` | id, opinion(选填) | 操作结果 | 县级业务人员操作 |
| 终审退回 | PUT | `/farm/transfer-records/final/reject` | id, opinion(必填) | 操作结果 | 县级业务人员操作 |
| 操作日志查询 | GET | `/farm/transfer-records/{id}/logs` | id | 操作日志列表 | 返回该记录全部审批操作记录 |
| 导出Excel | POST | `/farm/transfer-records/export` | 筛选参数、ids（可选） | Excel文件流 | - |

---

## 3.5 非功能需求

- **性能**：列表首屏加载 < 2秒（P95）；表单提交 < 3秒（P95）；Excel导出 < 10秒
- **安全与权限**：
  - 企业角色（ent_admin、ent_reporter）：仅本企业数据
  - 乡镇角色（town_vet_station、town_gov_staff）：本乡镇所有企业数据
  - 县级角色（county_admin、county_staff）：全县所有数据
- **兼容性**：支持Chrome 90+、Edge 90+、Firefox 88+；最低分辨率1280×768

---

## 3.6 验收标准

- [ ] 列表正确显示转栏信息记录，按创建时间倒序排列
- [ ] 各角色按数据权限仅看到对应范围数据（企业仅本企业、乡镇仅本乡镇、县级看全部）
- [ ] 企业名称、行政区划列在企业端角色不显示，监管端角色显示
- [ ] 企业名称、行政区划筛选条件在企业端角色不显示，监管端角色显示
- [ ] 状态、日期范围、源圈舍筛选均生效
- [ ] 新增弹窗中源圈舍选中后自动回显当前存栏量，只读不可编辑
- [ ] 目标圈舍与源圈舍相同时，即时显示红色提示，阻断提交
- [ ] 转栏数量超过源圈舍存栏量时，提交前显示错误提示（含具体数量）
- [ ] 保存草稿不校验必填，提交时全量校验
- [ ] 提交确认弹窗展示审批流程说明及上报类型（转栏信息）、源圈舍、目标圈舍、转栏日期
- [ ] 草稿/退回状态可编辑，其他状态只读
- [ ] 操作列按角色正确显示对应操作按钮
- [ ] 乡镇畜牧兽医站可对待核实数据执行核实通过/核实退回
- [ ] 乡镇政府可对待审核数据执行审核通过/审核退回
- [ ] 县级业务人员可对待终审数据执行终审通过/终审退回
- [ ] 县级管理员可直接新增数据（直接入库）、编辑/删除企业上报数据
- [ ] 查看详情正确分区展示所有字段
- [ ] 详情弹窗展示四步审批进度条：企业提交→乡镇畜牧兽医站核实→乡镇政府审核→县级终审入库
- [ ] 导出Excel包含所有规定字段（含企业名称、行政区划），状态列为中文
