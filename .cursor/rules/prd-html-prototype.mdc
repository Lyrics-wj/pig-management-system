---
alwaysApply: true
---

---

## PRD 转 HTML 原型规范

### 初次生成原型

1. 优先使用原生 HTML + CSS + 少量 JS，不引入重型框架
2. 允许使用 CDN 引入：Tailwind CSS、Font Awesome 图标、Alpine.js（轻量交互）
3. 所有功能区块必须设置语义化 `id`，格式：`#feature-xxx`，例如 `#feature-login`、`#feature-list`
4. 使用 `<!-- SECTION: 功能名称 -->` 注释标记每个主要区块的开始和结束
5. Mock 数据集中定义在 `<script>` 顶部的 `const MOCK_DATA = {}` 中，便于快速修改数据而不动结构

### 数据与交互约定

- 所有表单提交、按钮点击均用 `console.log` 或弹窗模拟，不做真实请求
- 状态切换优先用 CSS class 控制，避免复杂 JS 逻辑
- 列表数据从 `MOCK_DATA` 读取，修改数据只需改 `MOCK_DATA`，无需改 DOM 逻辑

---

## 沟通效率规范

- 用户说"改 xxx"时，直接定位到对应 `#id` 区块输出修改片段，无需确认
- 用户说"加 xxx 功能"时，先用一行说明插入位置，再给代码
- 用户说"全部重来"或"重新生成"时，才输出完整文件
- 遇到需求不明确时，提出**最多 1 个**关键问题，而非多个问题来回确认

---

## 文件组织约定

- 每个 PRD 功能模块对应一个独立 HTML 文件
- 文件命名格式：`XX-功能名称.html`（与 PRD 章节编号对应）
- 公共样式如需复用，提取到 `<style>` 块顶部并用注释标记 `/* SHARED STYLES */`
